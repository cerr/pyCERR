{
    "_comment": "urOMT model/algorithm settings. Data settings (scans, mask, voxel spacing, domain size) are read directly from planC and are NOT stored here. Mirrors cfg fields (b) from the MATLAB getParams.m.",

    "sigma": 2e-3,
    "dt": 0.4,
    "nt": 10,
    "alpha": 9000.0,
    "beta": 5000.0,
    "eta": 0.0,
    "niter_pcg": 60,
    "maxUiter": 6,
    "solver": "gn",
    "gnLambda0": 0.0,
    "gnPrecond": 0,
    "gnCgTol": 1e-2,
    "dTri": 1,
    "reinitR": 0,
    "smooth": 1,
    "smooth_method": "affine",
    "smooth_dt": 0.1,
    "do_resize": 0,
    "size_factor": 1.0,

    "convertToConc": 1,
    "normMethod": "RSE",
    "baselineFrames": 1,
    "T10": 2.1,
    "r1": 3.9,
    "basePts": 1,
    "TR": null,
    "FA": null,
    "conc_clip": null,

    "rhoScale": null,
    "bbox_pad": 0,
    "bbox_full_z": 0,

    "chiStructNum": null,

    "time": {
        "first_time": 1,
        "last_time": null,
        "time_jump": 1
    },

    "_field_doc": {
        "sigma": "diffusion coefficient (Gaussian bandwidth) in the advection-diffusion PDE",
        "dt": "time-step size for the inner numerical integration",
        "nt": "number of inner time sub-steps between two consecutive image frames",
        "alpha": "weight on the source-term penalty (Gamma2)",
        "beta": "weight on the final-image fidelity term (Gamma3)",
        "eta": "OPTIONAL weight on a velocity H1-smoothness penalty (Gamma4 = eta*hd*dt*sum|grad u|^2), a pyCERR extension with no counterpart in the reference MATLAB implementation. DEFAULT 0 = off, which keeps the objective identical to the reference. It was briefly defaulted to 1000 to tame an incoherent velocity field, but that incoherence turned out to be a symptom of a non-mass-conserving advection operator (since fixed); with the correct operator, neighbour-vector direction coherence is ~0.88 at eta=0 and rises only to ~0.93 by eta=1000, so the penalty is no longer needed. Enable it only if you deliberately want a smoother (biased) velocity field, e.g. for visualization",
        "niter_pcg": "max iterations for the (P)CG Hessian/diffusion solves",
        "maxUiter": "outer optimization iterations per time interval (for solver='gn' this is the number of Gauss-Newton steps; few iterations act as early-stopping regularization, as in the MATLAB)",
        "solver": "'gn' = exact Gauss-Newton block (matrix-free GN Hessian 2*beta*hd*J'J + regularization via the final-density sensitivities + CG + backtracking line search; this is the same GN Hessian as MATLAB GNblock_ur); 'lbfgs' = scipy L-BFGS-B on the adjoint gradient (first-pass)",
        "gnLambda0": "initial Levenberg-Marquardt damping for solver='gn', relative to the misfit Hessian scale 2*beta*hd; adaptively reduced on accepted steps and raised on rejected ones. DEFAULT 0 = undamped pure Gauss-Newton, matching the reference MATLAB GNblock_ur; raise only for conditioning if a problem will not converge",
        "gnPrecond": "1 = apply a Jacobi preconditioner (from the regularization+damping diagonal) to the Gauss-Newton CG solve. DEFAULT 0 = off, matching the reference implementation's plain pcg. Because diagR carries the factor alpha while diagU does not, the preconditioner is ~alpha stronger on the velocity block, so CG over-steps velocity and starves the source r: with it enabled the solver lands on a ~3.7x worse objective with the source-energy term ~500x too small",
        "gnCgTol": "relative residual tolerance for the Gauss-Newton CG solve (MATLAB pcg uses 1e-2)",
        "dTri": "boundary condition for advection: 1='closed', 3='open'",
        "reinitR": "0=initialize each interval with the previous evolved density (consecutive); 1=reset to the measured frame",
        "smooth": "total evolution TIME (t_tot) of the smoothing flow applied to each frame (0=off); the number of steps is round(smooth/smooth_dt), matching MATLAB affine_diffusion_3d(A, t_tot, dt, aff). NOTE: this is a time, not a step count",
        "smooth_method": "'affine' (affine-invariant mean-curvature flow, MATLAB affine_diffusion_3d), 'linear' (heat flow), or 'gaussian' (scipy stand-in)",
        "smooth_dt": "evolution step size for the affine/linear smoothing flow",
        "do_resize": "1=resize each frame/mask by size_factor",
        "size_factor": "resize multiplier when do_resize=1",
        "convertToConc": "1=concentration ('CC') normalization, 0=raw signal (used only when 'normMethod' is null)",
        "normMethod": "signal normalization for Part 1: 'CC' (contrast concentration, needs TR/FA), 'RSE' (relative signal enhancement S(t)/S0), 'none' (raw); null=derive from convertToConc",
        "rhoScale": "global rescale of the normalized density (urOMT velocity is scale-invariant, so this only conditions the solver): null/0=off, 'auto'=make the first-frame ROI mean 1 (useful for tiny CC concentrations), or a numeric multiplier",
        "bbox_pad": "pad the ROI bounding box by this many voxels per in-plane side (matches the MATLAB getRange; 0=tight box)",
        "bbox_full_z": "1=use the full z-extent of the scan for the ROI box instead of the mask's z-range (matches MATLAB getRange)",
        "T10": "pre-contrast longitudinal relaxation time (seconds) for concentration conversion (default 1/0.6)",
        "r1": "relaxivity for concentration conversion (default 3.8)",
        "basePts": "number of leading SELECTED frames consumed in-sequence as the pre-contrast baseline S(0) (only when baselineFrames=0 and the window starts at frame 1)",
        "baselineFrames": "external pre-contrast baseline: S(0)=mean of the first N temporally-ordered scans, NOT consumed (the whole selected window is transported). 1=use the 1st timepoint. Matches the MATLAB workflow; >0 overrides the in-sequence basePts consumption",
        "TR": "repetition time override in SECONDS; null=read scanInfo.repetitionTime (DICOM milliseconds, converted to seconds automatically)",
        "FA": "flip angle (degrees) override for concentration conversion; null=read from scan metadata (scanInfo.flipAngle)",
        "conc_clip": "optional [min,max] clip applied to the normalized signal (S(t)/S(0)) before concentration conversion; null=no clip",
        "chiStructNum": "structure index for the source-indicator chi (MATLAB K); null=K=1 everywhere (source r may act anywhere in the ROI)",
        "time": "1-based selection into the supplied scan list: first_time:time_jump:last_time (last_time=null -> last scan)"
    }
}
